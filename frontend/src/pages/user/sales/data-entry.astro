---
import AppLayout from '@/layouts/AppLayout.astro';
import Sidebar from '@/components/Sidebar';
import PageHeader from '@/components/PageHeader';
import DataEntryIsland from '@/components/DataEntryIsland';
import RouteGuard from '@/components/RouteGuard';
---

<AppLayout title="Veri Girişi - Aylık KPI">
  <div class="flex h-screen bg-gray-50">
    <!-- Sidebar -->
    <Sidebar role="user" client:only="react" />
    
    <!-- Main Content -->
    <div class="content-root flex-1 flex flex-col">
      <!-- Header with UserProfileDropdown -->
      <PageHeader client:only="react">
        <div>
          <h1 class="text-2xl font-bold text-gray-900">Veri Girişi - Aylık KPI</h1>
          <p class="text-sm text-gray-600 mt-1">Satış verilerinizi sisteme girin ve güncelleyin. Performans takibi için veri kayıtlarınızı yönetin.</p>
        </div>
      </PageHeader>

      <!-- Main Content Area -->
      <main class="flex-1 overflow-y-auto p-6 space-y-6">
        <RouteGuard routePath="/user/sales/data-entry" client:only="react">
          <!-- Üst kontrol çubuğu ve Dinamik KPI listesi (React parent island) -->
          <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-4">
            <DataEntryIsland categoryFilter="Satış - Aylık KPI" client:only="react" />
          </div>

        <!-- Alt panel ve kaydet butonu -->
        <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-4">
          <div class="min-h-[140px]"></div>
          <div class="mt-4 flex justify-end">
            <button class="inline-flex items-center space-x-2 px-4 py-2 rounded-full bg-emerald-600 text-white shadow hover:bg-emerald-700">
              <span>🔒</span>
              <span class="text-sm font-medium">Verileri Kaydet</span>
            </button>
          </div>
        </div>
        </RouteGuard>
      </main>
    </div>
  </div>
</AppLayout>
<!-- React island loads the brand list; inline scripts removed -->
---
import AppLayout from '@/layouts/AppLayout.astro';
import Sidebar from '@/components/Sidebar';
import PageHeader from '@/components/PageHeader';
import SalesDashboardIsland from '@/components/SalesDashboardIsland';
---

<AppLayout title="Kiralama Dashboard">
  <div class="flex h-screen bg-gradient-to-br from-gray-50 to-gray-100 transition-colors duration-300">
    <!-- Sidebar -->
    <Sidebar role="user" client:only="react" />

    <!-- Main Content Area -->
    <div class="content-root flex-1 flex flex-col">
      <!-- Page Header -->
      <PageHeader client:only="react">
        <div>
          <h1 class="text-2xl font-bold text-gray-900">Kiralama Dashboard</h1>
          <p class="text-sm text-gray-600 mt-1">Kiralama özetleri ve performans göstergeleri</p>
        </div>
      </PageHeader>

      <!-- Main Content -->
      <main class="flex-1 p-6 space-y-8 overflow-y-auto">
        <!-- Dashboard Content -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 transition-colors duration-300">
          <main class="flex-1 overflow-y-auto p-6">
            <!-- Kiralama verileri (aylık) gösterimi: DataEntry sayfasıyla aynı kategoride -->
            <SalesDashboardIsland client:only="react" categoryFilter="Kiralama - Aylık KPI" brandCategory="kiralama-markalari" />
          </main>
        </div>
      </main>
    </div>
  </div>
</AppLayout>